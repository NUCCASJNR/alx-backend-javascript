// Int8Array
const int8Array = new Int8Array([10, -20, 127, -128]);

// Uint8Array
const uint8Array = new Uint8Array([0, 255, 128]);

// Uint8ClampedArray
const uint8ClampedArray = new Uint8ClampedArray([-10, 255, 300]);
console.log(int8Array); 
